interface:
  display_name: "Adaptive Agent Orchestrator"
  short_description: "Reduce context, duplication, and elapsed task time"
  default_prompt: "Use $adaptive-agent-orchestrator to keep the main agent productive, isolate only independent work, and minimize repeated context."

policy:
  allow_implicit_invocation: true
